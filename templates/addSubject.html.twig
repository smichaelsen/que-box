{% extends 'base.html.twig' %}

{% block body %}
    <h2>Neues Fach anlegen:</h2>
    {{ form_start(newSubjectForm) }}
    {{ form_widget(newSubjectForm) }}
    <button type="submit">Speichern</button>
    {{ form_end(newSubjectForm) }}
{% endblock body %}