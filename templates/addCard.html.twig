{% extends 'base.html.twig' %}

{% block body %}
    <h2>Neue Karte anlegen:</h2>
    {{ form_start(newCardForm) }}
    {{ form_widget(newCardForm) }}
    <button type="submit">Speichern</button>
    {{ form_end(newCardForm) }}
{% endblock body %}